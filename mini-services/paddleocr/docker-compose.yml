services:
  paddleocr:
    build: .
    container_name: divisionjuridique-paddleocr
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      OCR_SERVICE_TOKEN: ${OCR_SERVICE_TOKEN}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s
