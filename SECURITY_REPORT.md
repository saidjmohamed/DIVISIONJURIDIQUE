# 🔐 SECURITY_REPORT — تقرير أمان مشروع DIVISIONJURIDIQUE

<div dir="rtl">

تاريخ التحليل: 2026-05-06
المُحلل: Super Z AI Agent

</div>

---

## 1. ملخص الأمان

| المقياس | القيمة |
|---------|--------|
| مستوى الأمان العام | ⚠️ متوسط (تم تحسينه من ضعيف) |
| ثغرات حرجة مكتشفة | 3 (تم إصلاحها) |
| ثغرات متوسطة | 4 |
| ثغرات منخفضة | 5 |
| مفاتيح API مدمجة في الكود | 0 (تم التنظيف سابقاً) |

---

## 2. الثغرات الأمنية

### 🔴 حرجة (تم إصلاحها)

#### 2.1 نقطة `/api/legal-search` بدون حماية

- **الوصف**: نقطة نهاية البحث القانوني كانت مفتوحة تماماً بدون أي مصادقة أو تقييد طلبات. تقوم الوظيفة بتحميل 268 قانون في الذاكرة (13MB+) وإجراء عمليات regex مكثفة.
- **الخطر**: إمكانية هجوم DoS عن طريق إرسال طلبات متكررة، مما يستهلك موارد الخادم ويؤثر على باقي المستخدمين.
- **الإصلاح**: إضافة Rate Limiting بحد 20 طلب/دقيقة باستخدام `checkRateLimit` من `ai-core.ts`.

#### 2.2 تسريب معلومات Redis في `/api/redis-check`

- **الوصف**: كانت نقطة النهاية تعرض أول 30 حرفاً من عنوان Redis URL عبر `url.slice(0, 30) + "..."`.
- **الخطر**: كشف بنية URL يمكن أن يساعد مهاجماً في تحديد مزود الخدمة والمنطقة الجغرافية.
- **الإصلاح**: إزالة عرض URL prefix تماماً + إضافة مصادقة CRON_SECRET في الإنتاج + Rate Limiting.

#### 2.3 مصادقة اختيارية في `/api/debug-ai`

- **الوصف**: كانت المصادقة تُفحص فقط إذا كان `CRON_SECRET` مضبوطاً — إذا كان فارغاً، تكون النقطة مفتوحة بالكامل وتعرض حالة جميع مزودي AI.
- **الخطر**: كشف معلومات حساسة عن حالة المزودين والنماذج المستخدمة.
- **الإصلاح**: تغيير المنطق ليتطلب CRON_SECRET دائماً — إذا لم يكن مضبوطاً يُرفض الوصول.

---

### 🟡 متوسطة (تحتاج مراجعة يدوية)

#### 2.4 تقييد الطلبات Fail-Open

- **الملف**: `src/lib/rate-limit.ts:52-54`
- **الوصف**: عندما يكون Redis غير متاح، يسمح النظام بجميع الطلبات (fail-open).
- **الخطر**: أثناء انقطاع Redis، لا يعمل Rate Limiting إطلاقاً.
- **التوصية**: مراعاة fail-closed للنقاط الحساسة أو إضافة تقييد محلي كـ backup.

#### 2.5 إعدادات أمان Gemini معطلة

- **الملف**: `src/lib/ai-core.ts:519-524`
- **الوصف**: جميع فئات أمان Gemini مضبوطة على `BLOCK_NONE`:
  ```
  HARM_CATEGORY_HARASSMENT → BLOCK_NONE
  HARM_CATEGORY_HATE_SPEECH → BLOCK_NONE
  HARM_CATEGORY_DANGEROUS_CONTENT → BLOCK_NONE
  HARM_CATEGORY_SEXUALLY_EXPLICIT → BLOCK_NONE
  ```
- **الخطر**: النموذج قد ينتج محتوى ضاراً أو غير لائق.
- **التوصية**: ضبط على `BLOCK_MEDIUM_AND_ABOVE` كحد أدنى، خاصة في تطبيق قانوني احترافي.

#### 2.6 ~20 تبعية غير مستخدمة تزيد مساحة الهجوم

- **الملف**: `package.json`
- **الوصف**: حزم مثل `next-auth`, `@mdxeditor/editor`, `sharp`, `z-ai-web-dev-sdk` مثبتة لكنها غير مستخدمة.
- **الخطر**: كل تبعية تزيد مساحة الهجوم — الثغرات في الحزم غير المستخدمة يمكن أن تُستغل.
- **التوصية**: إزالة جميع التبعيات غير المستخدمة.

#### 2.7 Prisma غير مستخدم لكنه مثبت

- **الملف**: `src/lib/db.ts`, `prisma/schema.prisma`
- **الوصف**: `db.ts` يصدر عميل Prisma لكن لا يتم استيراده في أي ملف آخر.
- **الخطر**: غير مباشر — يزيد حجم البناء ويعرض معلومات مخطط قاعدة البيانات.
- **التوصية**: إما تطوير واجهة المستخدمين أو إزالة Prisma بالكامل.

---

### 🟢 منخفضة

#### 2.8 `dangerouslySetInnerHTML` في layout.tsx

- **الملف**: `src/app/layout.tsx:69`
- **الوصف**: يُستخدم لتسجيل Service Worker — كود ثابت وآمن.
- **الخطر**: منخفض جداً — لا يدخل مدخلات المستخدم.

#### 2.9 `dangerouslySetInnerHTML` في chart.tsx

- **الملف**: `src/components/ui/chart.tsx:83`
- **الوصف**: مكون shadcn/ui — يحقن متغيرات CSS.
- **الخطر**: منخفض — مكون داخلي.

#### 2.10 TypeScript `any` في InstallPrompt

- **الملف**: `src/components/InstallPrompt.tsx:6,15,36`
- **الوصف**: استخدام `any` للتعامل مع `BeforeInstallPromptEvent` غير المدعوم رسمياً.
- **الخطر**: منخفض — كود واجهة المستخدم فقط.

#### 2.11 `ignoreBuildErrors: true` في next.config.ts

- **الملف**: `next.config.ts:6`
- **الوصف**: يخفي أخطاء TypeScript أثناء البناء.
- **الخطر**: قد يخفي أخطاء أمنية في الأنواع.

#### 2.12 سكريبتات Python بمسارات مطلقة

- **الملف**: `scripts/process_laws.py`, `scripts/process_laws_v2.py`
- **الوصف**: تستخدم مسارات مثل `/home/z/my-project/...`.
- **الخطر**: منخفض — سكريبتات مساعدة فقط، لا تُنفذ في الإنتاج.

---

## 3. حالة API Points الأمنية

| نقطة النهاية | المصادقة | Rate Limiting | الحالة |
|--------------|----------|---------------|--------|
| `/api/ai` | ❌ | ✅ 20/min | ✅ آمنة |
| `/api/legal-search` | ❌ | ✅ 20/min | ✅ تم الإصلاح |
| `/api/legal-updates` | ❌ | ❌ | ⚠️ بيانات عامة |
| `/api/petition-check` | ❌ | ✅ 10/min | ✅ آمنة |
| `/api/quiz/generate` | ❌ | ✅ 10/min | ✅ آمنة |
| `/api/tools/memo` | ❌ | ✅ 6/min | ✅ آمنة |
| `/api/tools/contract` | ❌ | ✅ 8/min | ✅ آمنة |
| `/api/tools/judgment` | ❌ | ✅ 8/min | ✅ آمنة |
| `/api/cron/fetch-updates` | ✅ CRON_SECRET | ❌ | ✅ آمنة |
| `/api/debug-ai` | ✅ CRON_SECRET | ❌ | ✅ تم الإصلاح |
| `/api/redis-check` | ✅ CRON_SECRET | ✅ 10/min | ✅ تم الإصلاح |
| `/api/telegram-sync` | ⚠️ مشروط | ❌ | ⚠️ مراجعة مطلوبة |

---

## 4. التوصيات الأمنية

### عالية الأولوية
1. **إزالة التبعيات غير المستخدمة** — تقليل مساحة الهجوم بـ 15-20MB
2. **تفعيل إعدادات أمان Gemini** — ضبط على `BLOCK_MEDIUM_AND_ABOVE`
3. **مراجعة `/api/telegram-sync`** — المصادقة اختيارية في التطوير

### متوسطة الأولوية
4. **إضافة CSP Headers** — حماية من XSS وحقن البرامج
5. **تقييد `/api/legal-updates`** — إضافة Rate Limiting
6. **إزالة `ignoreBuildErrors`** — كشف أخطاء الأنواع

### منخفضة الأولوية
7. **إضافة HSTS Header** — فرض HTTPS
8. **تدوير مفاتيح API** — تغيير أي مفتاح كان معرضاً في تاريخ Git
9. **تحديث سكريبتات Python** — استخدام مسارات نسبية

---

## 5. التحقق من الترميز

- ✅ جميع ملفات JSON (388 ملف) تم فحصها — لا توجد مشاكل ترميز UTF-8
- ✅ لا توجد علامات BOM في أي ملف
- ✅ النصوص العربية تظهر بشكل صحيح
- ✅ لا توجد أخطاء JSON.parse في أي ملف

---

## 6. التحقق من البيانات الحساسة

- ✅ لا توجد مفاتيح API مدمجة في الكود المصدري
- ✅ جميع المفاتيح تُقرأ من `process.env`
- ✅ ملف `.env.example` لا يحتوي على قيم حقيقية
- ✅ ملف `.gitignore` يستثني جميع ملفات البيئة
- ⚠️ يجب تدوير المفاتيح التي كانت معرضة في تاريخ Git (تم التنظيف سابقاً)
