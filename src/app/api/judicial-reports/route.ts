import { NextResponse } from 'next/server';
const URL=process.env.SUPABASE_URL||'https://kgsmunxqctpptxljizmy.supabase.co';
const KEY=process.env.SUPABASE_PUBLISHABLE_KEY||'sb_publishable_-dakDRrLhmOuu3b1TkTBfg_b5-1bLEr';
export async function POST(req:Request){try{const b=await req.json();const institutionId=typeof b?.institutionId==='string'?b.institutionId:null;const contactId=typeof b?.contactId==='string'?b.contactId:null;const reportType=typeof b?.reportType==='string'?b.reportType.trim():'other';const message=typeof b?.message==='string'?b.message.trim():'';if(!message&&!['number_invalid','number_not_working'].includes(reportType))return NextResponse.json({error:'الرسالة مطلوبة'},{status:400});const r=await fetch(`${URL}/rest/v1/judicial_reports`,{method:'POST',headers:{apikey:KEY,Authorization:`Bearer ${KEY}`,'Content-Type':'application/json','Prefer':'return=representation'},body:JSON.stringify({institution_id:institutionId,contact_id:contactId,report_type:reportType,message}),cache:'no-store'});if(!r.ok)return NextResponse.json({error:'تعذر إرسال البلاغ'},{status:502});return NextResponse.json({ok:true});}catch{return NextResponse.json({error:'طلب غير صالح'},{status:400})}}
